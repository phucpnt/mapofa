/**
 * Created by phucpnt on 5/24/16.
 */

export default function createBacklogByComment(podio, {appId, appField, workspaceId}){
  // retrieve top 100 notifications
  // indentify the correct request comment
  // process the request comment -> create backlog -> assign suggested person first.
  // add description with links to request referenced item.
}